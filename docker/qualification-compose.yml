services:
  app:
    volumes:
      - qualification_models:/models
      - projects:/data/projects

  frontend:
    volumes:
      - ./web_app:/app
      - qualification_frontend_node_modules:/app/node_modules

volumes:
  qualification_models:
    name: ${TICKET27_MODELS_VOLUME}
  qualification_frontend_node_modules:
    name: ${TICKET27_FRONTEND_VOLUME}
